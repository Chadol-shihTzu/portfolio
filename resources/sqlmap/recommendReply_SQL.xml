<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="recommendboard_reply">
	<!-- namespace에는 테이블명추가 -->
	<!-- 게시물 리스트 -->
	<select id="replyList" parameterType="int" resultType="com.portfolio.jdh.domain.recommendReplyVO">
	<![CDATA[ 
		select recommendRno, recommendNo, writer, content, regDate 
		from recommendboard_reply 
		where recommendNo = #{recommendNo} 
	]]>
	</select>
	
	<!--  댓글 작성  -->
	<insert id="replyWrite" parameterType="com.portfolio.jdh.domain.recommendReplyVO">
	<![CDATA[ 
	insert into recommendboard_reply(recommendNo, writer, content) 
	value(#{recommendNo}, #{writer}, #{content}) 
	]]>
	</insert>
	
	<!--  댓글 수정  -->
	<update id="replyModify" parameterType="com.portfolio.jdh.domain.recommendReplyVO">
	<![CDATA[ 
	update recommendboard_reply set writer = #{writer}, content = #{content} 
	where recommendRno = #{recommendRno} and recommendNo = #{recommendNo} 
	]]>
	</update>
	
	<!--  댓글 삭제  -->
	<delete id="replyDelete" parameterType="com.portfolio.jdh.domain.recommendReplyVO">
	<![CDATA[ 
	delete from recommendboard_reply 
	where recommendRno = #{recommendRno} 
	]]>
	</delete>
	
	<!--  단일댓글조회  -->
	<select id="replySelect" parameterType="com.portfolio.jdh.domain.recommendReplyVO" resultType="com.portfolio.jdh.domain.recommendReplyVO">
	<![CDATA[ 
	select recommendRno, recommendNo, writer, content, regDate 
	from recommendboard_reply 
	where recommendNo=#{recommendNo} and recommendRno= #{recommendRno} 
	]]>
	</select>
</mapper>
